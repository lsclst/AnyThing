<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
             xmlns:app="http://schemas.android.com/apk/res-auto"
             android:layout_width="match_parent"
             android:layout_height="match_parent"
             android:orientation="vertical">


	<ImageView
		android:id="@+id/id_iv_splash"
		android:layout_width="match_parent"
		android:layout_height="match_parent"
	android:scaleType="centerCrop"/>

	<LinearLayout android:layout_width="wrap_content"
	              android:layout_height="wrap_content"
	              android:layout_gravity="bottom|right"
	              android:layout_marginBottom="8dp"
	              android:layout_marginRight="8dp"
	              android:orientation="horizontal">
		<ImageView android:id="@+id/id_splash_dl"
		           android:layout_width="@dimen/splash_btn_size"
		           android:layout_height="@dimen/splash_btn_size"
		           android:layout_marginRight="16dp"
		           android:clickable="true"
		           android:src="@drawable/ic_download_splash"
		/>
		<ImageView
			android:id="@+id/id_splash_like"
			android:layout_width="@dimen/splash_btn_size"
			android:layout_height="@dimen/splash_btn_size"
			android:clickable="true"
			android:src="@drawable/ic_like_splash"
		/>
	</LinearLayout>
	<TextView
		android:id="@+id/id_splash_app"
		android:layout_width="match_parent"
		android:layout_height="match_parent"
		android:layout_gravity="center"
		android:gravity="center"
		android:text="@string/app_name"
		android:textColor="@android:color/white"
		android:textSize="24sp"
		android:visibility="gone"
	/>
</FrameLayout>